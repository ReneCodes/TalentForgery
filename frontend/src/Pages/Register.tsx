import {FC} from 'react';
import {Button} from '@mui/material';

export const Register: FC = () => {
	return (
		<div>
			<div>Register</div>
			<Button
				variant="contained"
				color="success">
				Success
			</Button>
		</div>
	);
};
